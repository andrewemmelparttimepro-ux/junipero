import SwiftUI

// Badge removed per design direction. Struct kept as empty
// so any stale references compile without error.

struct HermesBadge: View {
    var body: some View {
        EmptyView()
    }
}
